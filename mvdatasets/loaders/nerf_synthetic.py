def load_dtu(
    data_path,
    load_with_mask=True,
    rotate_scene_x_axis_degrees=0,
    scene_scale_multiplier=0.25,  # keeps the object within the unit sphere
    # downscale_factor=1
    device="cpu",
):
    pass
